{
  "name": "fuzzy-test",
  "version": "1.0.0",
  "description": "Reads cucumber json output and a config file. Passes or fails the cucumber json based on configured thresholds.",
  "bin": {
    "fuzzy-test": "src/cli.js",
    "synchroniseTestPlan": "src/cli-synchronise.js",
    "exportFeatures": "src/cli-export.js"
  },
  "main": "./src/cli.js",
  "scripts": {
    "test": "mocha \"src/**/*.test.js\"",
    "coverage": "nyc --all --reporter=html mocha \"src/**/*.test.js\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.2.0",
    "chai-arrays": "^2.0.0",
    "mocha": "^5.2.0",
    "mock-fs": "^4.7.0",
    "nock": "^10.0.6",
    "nyc": "^13.1.0",
    "proxyquire": "^2.1.0",
    "sinon": "^7.2.3",
    "sinon-chai": "^3.3.0"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "commander": "^2.19.0",
    "hyperdiff": "^2.0.5"
  }
}
